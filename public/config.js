// EmailJS configuration.
// Get these 3 values from https://dashboard.emailjs.com after creating a free
// account, adding a Gmail service, and creating an email template.
// These keys are SAFE to be public (EmailJS public key is domain/rate limited).
window.EMAILJS_CONFIG = {
  publicKey:  'YOUR_PUBLIC_KEY',
  serviceId:  'YOUR_SERVICE_ID',
  templateId: 'YOUR_TEMPLATE_ID',
};
